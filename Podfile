# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'date' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
	pod 'DLLocalNotifications'
  # Pods for date

  target 'dateTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'dateUITests' do
    # Pods for testing
  end

end
